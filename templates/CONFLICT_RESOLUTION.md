# 行冲突解决记录 — <分支名> ← <主干>

> 由 qiq-alignmain Phase 4 产出。每个 git 冲突块（hunk）一条，记录双方意图与在两条硬约束下的解决方案。

## 两条硬约束（每块都必须核对）

- **约束①｜不回滚已有逻辑**：主干侧 + 分支侧的有效行为都保留。
- **约束②｜实现分支本次目标**：分支这次准备新增/修改的逻辑完整保留。
- 两者无法同时满足 → `NEEDS-HUMAN`，交人工，不擅自二选一。

## 冲突文件清单

- `<file1>`（n 处冲突块）
- `<file2>`（n 处冲突块）

## 逐块记录

### CR-1 · `<file>` · 块 @<行号范围>
- **分支侧（HEAD/ours）意图**：
- **主干侧（origin/<主干>/theirs）意图**：
- **解决策略**：融合双方 / 以主干新形态承载分支意图 / NEEDS-HUMAN
- **解决后代码摘要**：
- **满足约束①（不回滚）**：✅ / ❌
- **满足约束②（实现分支目标）**：✅ / ❌
- **是否需人工**：否 / ⚠️ NEEDS-HUMAN（原因：保留 A 则丢 B …）

### CR-2 · `<file>` · 块 @<行号范围>
- ……

## 收尾核对

- [ ] `git diff --name-only --diff-filter=U` 为空（所有冲突已解决）
- [ ] 无整文件 `--ours/--theirs` 取舍（如有，已逐块记录理由）
- [ ] 无 `NEEDS-HUMAN` 残留（否则不提交，交人工）
- [ ] 已 `git add` + 完成 merge commit（commit hash：`<hash>`）

## NEEDS-HUMAN 待裁决项

- （列出所有需人工拍板的冲突块；为空写"无"）
